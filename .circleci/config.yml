version: 2
jobs:
  build:
    docker:
      - image: circleci/node
    steps:
      - checkout
      - run: sudo npm install -g yarn semantic-release@12.0.0
      - run: yarn install
      - run:
          name: test
          command: |
            mkdir -p ~/reports/graphql-import
            yarn test
      - store_test_results:
          path: ~/reports/graphql-import
      - store_artifacts:
          path: ~/reports/graphql-import
      - run: semantic-release
      - run: yarn docs
      # needs proper NOW setup first
      # - run: yarn docs:publish
